#include <iostream>
#include <stdio.h>

int brute_force(int* arr, int s, int sum) {
  if(s == sum) return 1;
  else {
    
  }
}

int main() {
  ios_base::sync_with_stdio(false);
  int N, s, sum, i;
  cin>>N>>s;
  int arr[N];
  for(i=0; i<N; i++) cin>>arr[i];
  
}
